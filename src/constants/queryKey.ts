export const users = {
  info: ["users"],
  checkEmail: (email: string) => ["email", email],
};

export const search = {
  result: (keyword: string) => ["search", keyword],
};

export const diary = {
  list: ["diary", "list"],
};
