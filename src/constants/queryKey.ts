export const users = {
  info: ["users"],
  checkEmail: ["email"],
};

export const search = {
  search: (keyword: string) => ["search", keyword],
};

export const diary = {
  list: ["diary", "list"],
};
